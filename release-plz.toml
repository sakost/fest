# release-plz configuration
# https://release-plz.ieni.dev/docs/config

[workspace]
# Skip crates.io publish (git deps not allowed; distribute via PyPI instead)
publish = false
# Enable semver compatibility checks
semver_check = false
# Generate changelogs from conventional commits
changelog_update = true
# Git tag format
git_tag_name = "v{{ version }}"

[[package]]
name = "fest"
# Enable changelog for the package
changelog_update = true
# Include conventional commits in changelog
changelog_include = ["feat", "fix", "docs", "perf", "refactor", "build", "ci"]
